services:
  bmc:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${BMC_PORT:-8080}:${BMC_PORT:-8080}"
    volumes:
      - bmc-data:/app/data
    restart: unless-stopped
    environment:
      - Example=${Example:-8080}

volumes:
  bmc-data:
