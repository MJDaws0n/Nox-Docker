# How to use
1. Downloading this.
2. Run nox init, locally to setup the evironment and the the libraries conf file, you can then set up your app. Optionally you can just create the libraries.conf file with the libraries.
3. Create a .gitignore adding lib to it
4. Write code in main.nox.
5. Run it
